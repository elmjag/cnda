# cnda create <environment>

Create conda environment.

If <envirtonment> argument is a conda environment definition file, create that defined environment.
Otherwise create an empty environmet named <environment>.

